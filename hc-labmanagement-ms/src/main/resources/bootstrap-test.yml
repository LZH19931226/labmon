spring:
  cloud:
    nacos:
      config:
        server-addr: http://cdh02:8848
        file-extension: yaml
        prefix: hc-labmanagement-ms
        group: DEFAULT_GROUP
        namespace: dev
      discovery:
        server-addr: http://cdh02:8848
        namespace: dev
        ip: http://cdh03
