#此配置缺省使用 local profile便于于本地调试。
spring:
  profiles:
    active: dev
  main:
    allow-bean-definition-overriding: true
feign:
  client:
    config:
      default:
        connect-timeout: 600000
        read-timeout: 60000

